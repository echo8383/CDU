# 2026-09-16 运行与论文同步

## 最新覆盖说明：16:19

用户已确认 cap=2048 加速稳健性分支。旧全量等待队列 45200 已取消，
新进程 42832 正在执行 matched logistic + HGB，测试全量，主实验不变。
下文原始队列信息为切换前快照；不要重新启动全量等待队列。
新日志：protocol_fast_results/logs/probe_cap2048_20260916_161904.stdout.log。
恢复命令：python -u scripts/run_probe_robustness.py --training-cap 2048 --resume。

## 已完成

- 九个 detector 的 Fast 主实验全部为 350 条序列、23 个来源。保留原主结果，不重训、不重算主 probe。
- Fast duplicate 完成，CDU = 0.00000461124238148 bits；
  95% CI = [-0.00000126257019530, 0.0000124380353778]，包含零。
- 核对 duplicate 的逐折身份、训练/测试来源隔离、固定 C、共享基线、
  逐序列损失差与来源级 Bootstrap，已同步中英文正文。
- 来源影响敏感性仅重聚合既有 OOF 损失，没有重新拟合，详见
  paper/evidence/fast_controls/SOURCE_INFLUENCE.csv。
- 第二 probe 的四个小型实现测试通过，不等同于正式数据验证完成。

## 当前后台任务

- 38984：Fast noise（从 18/23 续跑）→ alpha2。
- 41900：检测到完整控制汇总后，核对并更新 paper/evidence、
  中英文 LaTeX 片段、main.pdf 与 main_zh.pdf。
- 45200：等待 38984 退出且内存足够后，自动运行全量 HGB 第二 probe。

以上 PID 仅用于本次运行，不应在未来盲目复用。
没有启动新的 detector；第二 probe 只读取冻结分数。

## 查看进度

~~~powershell
Get-Content protocol_fast_results/logs/resume_controls_20260916_155819.stdout.log -Tail 15 -Wait
~~~

第二 probe：

~~~powershell
Get-Content protocol_fast_results/logs/second_probe_20260916.stdout.log -Tail 15 -Wait
~~~

## 中断后恢复（先确认没有同名任务仍在运行）

~~~powershell
python -u scripts/run_protocol_fast.py --controls noise alpha2 --resume --continue-on-error
python scripts/sync_fast_controls_paper.py
python -u scripts/run_probe_robustness.py --training-cap 0 --resume
python scripts/summarize_probe_robustness.py --training-cap 0
~~~

## 尚未完成 / 不应提前宣称

- Fast noise 与 alpha2：以各自完整 SUMMARY.csv 为准，不以旧 QUEUE_STATUS.json 为准。
- HGB 全量实际耗时尚无测量值；第二 probe 当前为排队，而非已完成。
- 训练抽样 2048 点加匹配 logistic 是待用户选择的加速选项，默认队列未采用。
- basis-family sensitivity 尚未执行。本轮优先三控制和第二 probe，未扩展新的 detector 或 Stage 3。
- 自动写入新的完整 robustness 数字后，需要最终检查正文页数和排版。
- 作者、单位、联系信息仍需填写。

本次结果验证沿用数据验证技能的证据核对原则；交付遵循用户指定的现有
LaTeX/PDF 论文目录，不另建网页报告，不改变既有指标定义或结果。
